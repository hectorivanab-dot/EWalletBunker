services:
  app:
    build: .
    container_name: ewallet-bunker

    ports:
      - "8081:8081"

    volumes:
      - .:/app
      - /app/node_modules

    environment:
      - NODE_ENV=development
      - NGROK_AUTHTOKEN=TU_TOKEN_AQUI

    stdin_open: true
    tty: true
